function [ C, T ] = get_HBF_parameters( S )
%get_HBF_parameters = get C's, t's assuming M=I
%   get C's, t's by gradient descent assuming M=I
%   S is the training data set dim = (dim_data, num_data) = (1xD, N)
N = size(S);

end

