function [ output_args ] = compute_Hf( c, t, lambda )
%compute_Hf - computes regularized cost H[F] = sum (y_i, f(x_i))^2 +
%lambda || Pf ||^2
%   Inputs:
%       c - weights (K x 1)
%       t = centers (D  x K)
%   Outputs:
%       


end

